package cn.thinking.system.sequence.service;

import cn.thinking.org.system.sequence.Sequence;

public interface SequenceService {
    int getCurrent(String seqName);

    int getNextVal(String seqName);

    void setVal(Sequence sequence);

    void insertSequence(Sequence sequence);
}
